{% extends "base.html" %}

{% import "_macros.html" as macros %}

{% block content %}
<div class="container">
    {% for movie in movies %}
    <div class="page-header">
        <h1>
	        <a href="{{ url_for("main.movie", id=movie.id) }}">{{ movie.title }}</a>
        </h1>
        <p>{{ movie.description }}<p>
        <p><strong>评分：{{ movie.score }}</strong></p>
    </div>      
    {% endfor %}
    
    <div style="margin: auto;">
        {% if pagination %}
            <div class="pagination">
                {{ macros.pagination_widget(pagination, 'main.index') }}
            </div>
        {% endif %}
    </div>
    
</div>
{% endblock %}


